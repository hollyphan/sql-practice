cat > README.md << 'EOF'

# SQL Practice

Daily SQL practice working toward analytics engineering.

## Structure

- `fundamentals/` — SELECT, WHERE, GROUP BY, JOIN, aggregations, string/date functions

- `intermediate/` — CTEs, window functions, subqueries

- `advanced/` — complex multi-step queries, performance thinking

- `challenges/` — LeetCode SQL 50 and timed problem sets

## Stack

- MySQL (local)

- TablePlus

## Goal

Solve 3 LeetCode Medium problems involving CTEs and window functions back-to-back without documentation.

